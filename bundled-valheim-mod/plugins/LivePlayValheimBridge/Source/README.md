# LivePlay Valheim Bridge

Este pacote já contém o BepInExPack Valheim correto.

Ainda falta compilar `LivePlayValheimBridge.cs` para gerar:

`plugins/LivePlayValheimBridge.dll`

Depois de compilar, coloque a DLL nessa pasta `plugins/` antes de empacotar no app.
